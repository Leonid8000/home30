<header>
    <nav class="navbar navbar-expand-lg navbar-light bg-dark">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li>
                    <p class="nav-link text-light logo">Admin<span>Panel</span></p>
                </li>
                <li class="nav-item active">
                    <a class="nav-link text-light" href="{{ path('admin') }}">Home <span class="sr-only">(current)</span></a>
                </li>

                {# Show small screen #}
                <li class="nav-item">
                    <a class="nav-link text-light header-navigation" href="{{ path('question/create') }}">Create New Question</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-light header-navigation" href="{{ path('answer/create') }}">Create New Answer</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-light header-navigation" href="{{ path('questions') }}">Questions</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-light header-navigation" href="{{ path('answers') }}">Answers</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-light header-navigation" href="{{ path('users') }}">Users</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-light header-navigation" href="{{ path('results') }}">Poll Result</a>
                </li>

                {% if is_granted('ROLE_ADMIN')  %}
                    <li class="nav-item">
                        <a class="nav-link text-light" href="{{ path('app_logout') }}">Logout</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </nav>
</header>